using System.ComponentModel.DataAnnotations;

namespace BiebCWebsite.Models
{
    public class Amonoment
    {
        [Key]
        public int Id { get; set; }

        // abbonoment info toevoegen

        // seeding = 4 rijen zie beoordeling (kan 9999 zijn als die er boven zit maak if statment zovan is oneindig)
        //
    }
}
